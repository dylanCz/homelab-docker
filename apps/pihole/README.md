# Pihole
Pihole in this setup acts as our DNS server as well as ad blocker. It combines with Traefik to allow DNS routing via docker labels. In order to add a new service, we must add it as a local dns entry pointing at our hosts local IP address.
